# Verified installable Sep 2026 (see README for how these were checked).
# strands-agents[cedar] pulls in cedarpy for the CedarAuthorization
# intervention. The extras list below covers every provider main.py knows
# about; drop the ones you don't intend to use to trim the install.
#   openai   -> groq (verified end-to-end; Groq exposes an OpenAI-compat API)
#   ollama   -> local llama3.1 / qwen2.5 (see README caveats)
#   anthropic -> Claude
#   gemini   -> Google
strands-agents[cedar,openai,ollama,anthropic,gemini]

opensearch-py>=3.0.0
sentence-transformers>=5.0.0
